schema: "./services/core/schemas/**/*.graphql"
generates:
  ./services/core/schemas/graphqlApiTypes.d.ts:
    plugins:
      - typescript
      - typescript-resolvers
    config:
      maybeValue: T | undefined
